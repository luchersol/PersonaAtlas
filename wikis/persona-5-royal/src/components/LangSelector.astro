---
// src/components/LangSelector.astro

// 1. Obtenemos la URL actual procesada en el servidor
const { pathname } = Astro.url;

// 2. Desestructuramos la ruta para identificar el idioma y el destino actual
// Si estás en "/es/articulos/persona-4", segmentos será ["", "es", "articulos", "persona-4"]
const segmentos = pathname.split('/');
const currentLang = segmentos[1] || 'es'; 
const restoDeLaRuta = segmentos.slice(2).join('/'); 

// 3. Helper para recalcular la ruta exacta cambiando únicamente el prefijo de idioma
const getLangPath = (targetLang: string) => {
  return `/${targetLang}/${restoDeLaRuta}`;
};
---

<div class="lang-selector">
  <a 
    href={getLangPath('es')} 
    class={currentLang === 'es' ? 'active' : ''}
  >
    <span>ES</span>
  </a>
  <a 
    href={getLangPath('en')} 
    class={currentLang === 'en' ? 'active' : ''}
  >
    <span>EN</span>
  </a>
</div>

<style>
  /* CONTAINER DEL SELECTOR DE IDIOMA */
  .lang-selector {
      display: flex;
      gap: 10px;
      font-family: 'Impact', 'Arial Black', sans-serif;
  }
  
  /* ENLACE DE IDIOMA BASE (Adaptado con tus propiedades estéticas de P5) */
  .lang-selector a {
    display: inline-block;
    background-color: #000;
    color: #FFFFFF;
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: 900;
    padding: 6px 16px;
    border: 3px solid #000;
    transform: skewX(-15deg);
    box-shadow: 4px 4px 0px #333333;
    transition: all 0.15s ease-in-out;
    cursor: pointer;
    letter-spacing: 1px;
  }

  /* ESTADO EN FOCUS O HOVER */
  .lang-selector a:hover {
    background-color: #FFFFFF;
    color: #000;
    border-color: #FFFFFF;
    box-shadow: 4px 4px 0px #000;
    transform: skewX(-15deg) scale(1.05);
  }

  /* IDIOMA ACTIVO */
  .lang-selector a.active {
    background-color: var(--primary); /* Amarillo chillón de Persona 5 */
    color: #000;
    border-color: #000;
    box-shadow: 5px 5px 0px #000;
    animation: p5-pulse 1.5s infinite alternate ease-in-out;
  }

  /* Ajuste de alineación del texto */
  .lang-selector a span, 
  .lang-selector a {
    text-align: center;
  }

  /* ANIMACIÓN DE PARPADEO SUTIL PARA EL IDIOMA ACTIVO */
  @keyframes p5-pulse {
    0% {
      box-shadow: 4px 4px 0px #000;
      filter: brightness(1);
    }
    100% {
      box-shadow: 7px 7px 0px #e60012; /* Sombra roja */
      filter: brightness(1.15);
    }
  }

  /* ADAPTACIÓN PARA DISPOSITIVOS MÓVILES */
  @media (max-width: 600px) {
    .lang-selector {
      position: relative;
      top: 0;
      right: 0;
      justify-content: center;
      margin-bottom: 1.5rem;
    }
    
    .lang-selector a {
      font-size: 0.95rem;
      padding: 4px 12px;
    }
  }
</style>