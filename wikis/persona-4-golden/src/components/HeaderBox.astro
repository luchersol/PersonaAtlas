---
import NoteBox from './NoteBox.astro';


interface Props {
  title: string; // Asegúrate de que no diga "never" o un tipo incompatible
  subtitle: string;
  description?: string;
  mechanic?: string;
}

const {title, subtitle, description, mechanic} = Astro.props;
---

<header class="p4-page-header">
    <div class="p4-header-content">
        <div class="p4-header-text">
            <h1 class="p4-title">{title}</h1>
            <p class="p4-subtitle">{subtitle}</p>
        </div>
        {
           description && mechanic && <NoteBox {...{description, mechanic}} />
        }
    </div>
</header>

<style>
    .p4-page-header {
        border-left: 8px solid var(--primary);
        padding-left: 1.5rem;
    }

    .p4-header-content {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 1rem;
    }

    .p4-header-text {
        flex: 1;
    }

    .p4-subtitle {
        font-size: 1.2rem;
        color: var(--primary);
        text-transform: uppercase;
        font-weight: 700;
        margin-top: -0.5rem;
    }
</style>