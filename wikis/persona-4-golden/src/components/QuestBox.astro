---
import type { P4Language, DefaultParams } from '@types2/p4';

interface Props {
    id: number;
    name: string; 
    title: string;
    day: string;
    client: string; 
    location: string;
};

export function getStaticPaths() {
  return [
    { params: { lang: 'es' } }, 
    { params: { lang: 'en' } }
  ];
}

const { lang } = Astro.params as DefaultParams;

const {id, name, title, day, client, location} = Astro.props;
---
<div class="p4-quest-item">
    <div class="p4-quest-header">
        <span class="p4-quest-number">{name}</span>
        <span class={"p4-quest-day"}>{day}</span>
    </div>

    <div class="p4-quest-body">
        <div class="p4-quest-info">
            <h3 class="p4-quest-title">{title}</h3>
            <div class="p4-quest-meta">
                <p>👤 <strong>Client:</strong> {client}</p>
                <p>📍 <strong>Location:</strong> {location}</p>
            </div>
        </div>
        <a href={`/${lang}/quests/${id}`} class="p4-view-quest-btn" aria-label="View details">
            <svg class="p4-view-quest-icon" viewBox="0 0 24 24" aria-hidden="true">
                <circle cx="11" cy="11" r="5.5" />
                <path d="M15 15l4 4" />
            </svg>
        </a>
    </div>
</div>

<style>
    .p4-quest-item {
    background-color: #111;
    border: 2px solid #333;
    padding: 1.5rem;
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .p4-quest-item:hover {
    border-color: var(--color-orange);
  }

  .p4-quest-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .p4-quest-number {
    font-family: 'Impact', sans-serif;
    color: var(--color-orange);
    font-size: 1.2rem;
    letter-spacing: 1px;
  }

  .p4-quest-day {
    padding: 2px 10px;
    font-size: 0.8rem;
    background-color: #9e9e9e; 
    color: #fff; 
    text-transform: uppercase;
    font-weight: bold;
    transform: skewX(-10deg);´
  }

  .p4-quest-body {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
  }

  .p4-quest-info {
    flex: 1;
  }

  .p4-quest-title {
    font-size: 1.4rem;
    color: #fff;
    margin: 0.2rem 0;
  }

  .p4-quest-meta p {
    font-size: 0.95rem;
    color: #aaa;
    margin: 0.2rem 0;
  }

  .p4-view-quest-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.4rem;
    height: 2.4rem;
    background-color: var(--color-orange);
    color: var(--background);
    text-decoration: none;
    font-weight: bold;
    transform: skewX(-12deg);
    border: 2px solid #333;
    flex-shrink: 0;
  }

  .p4-view-quest-btn:hover { background-color: #fff; }

  .p4-view-quest-icon {
    display: block;
    width: 1.5rem;
    height: 1.5rem;
    stroke: var(--background);
    stroke-width: 2;
    fill: none;
    stroke-linecap: round;
    stroke-linejoin: round;
    transform: skewX(12deg);
  }
</style>