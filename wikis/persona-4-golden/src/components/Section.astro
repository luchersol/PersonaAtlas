---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---
<section class={`p4-section ${className}`}>
    <slot/>
</section>


<style>
    .p4-section { 
        display: flex; 
        flex-direction: column; 
        gap: 1rem; 
    }
</style>