---
import Layout from '@layouts/Layout.astro';
import { useTranslations, getSocialLinksList } from '@i18n/utils';
import type { P4SocialLink, Params } from '@types2/p4';
import SocialLinkTable from '@components/SocialLinkTable.astro';

export async function getStaticPaths() {
  const paths: any[] = [];
  const languages = ['en', 'es'] as const;

  for (const lang of languages) {
    const list = await getSocialLinksList(lang);
    
    list.forEach((char: P4SocialLink) => {
      // Extraemos el slug o ID de la ruta (ej: 'yosuke-hanamura')
      const character = char.route.split('/').pop(); 
      
      paths.push({
        params: { character, lang },
        props: { char },
      });
    });
  }

  return paths;
}


const { lang } = Astro.params as Params; 
const { char } = Astro.props;

const t = useTranslations(lang);
---

<Layout title={`${char.name} Profile | P4`}>
  <div class="p4-profile-wrapper">
    
    <!-- Botón volver atrás rápido -->
    <a href={`/${lang}/social-links`} class="p4-btn-back-list">{t("social-links.[character].back")}</a>

    <div class="p4-profile-layout">
      <!-- LADO IZQUIERDO: Imágenes de cartas y Rostro -->
      <div class="p4-profile-media">
        <div class="p4-big-avatar">
          <img src={char.image} alt={char.name} onerror="this.style.backgroundColor='#222'" />
        </div>
        <div class="p4-big-arcana">
          <img src={char.arcanaCard} alt={char.arcana} onerror="this.style.display='none'" />
          <div class="p4-arcana-title-badge">{char.arcana} Arcana</div>
        </div>
      </div>

      <!-- LADO DERECHO: Datos internos -->
      <div class="p4-profile-details">
        <h1 class="p4-title">{char.name}</h1>
        <div class="p4-meta-box">
          <p><strong>{t("social-links.[character].meta-box.text")}:</strong> {char.location}</p>
        </div>
        
        <div class="p4-bio-box">
          <h2>{t("social-links.[character].bio-box.text")}</h2>
          <p>{char.description}</p>
        </div>

        <SocialLinkTable ranks={char.ranks} lang={lang} />
      </div>
    </div>

  </div>
</Layout>

<style>
  .p4-profile-wrapper {
    margin-top: 1rem;
  }
  .p4-btn-back-list {
    color: #FFEB3B;
    text-transform: uppercase;
    font-weight: 700;
    text-decoration: none;
    font-size: 0.9rem;
    display: inline-block;
    margin-bottom: 1.5rem;
  }
  .p4-btn-back-list:hover {
    text-decoration: underline;
  }

  .p4-profile-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    background-color: #000;
    border: 4px solid #FFEB3B;
    padding: 2.5rem;
    box-shadow: 10px 10px 0px #000;
  }

  @media (min-width: 768px) {
    .p4-profile-layout {
      grid-template-columns: 250px 1fr;
    }
  }

  /* Media Styles */
  .p4-profile-media {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }
  .p4-big-avatar {
    width: 180px;
    height: 180px;
    border: 4px solid #fff;
    outline: 4px solid #000;
    transform: rotate(-2deg);
    background-color: #222;
  }
  .p4-big-avatar img { width: 100%; height: 100%; object-fit: cover; }
  
  .p4-big-arcana {
    text-align: center;
  }
  .p4-big-arcana img {
    max-width: 100px;
    height: auto;
    margin-bottom: 0.5rem;
  }
  .p4-arcana-title-badge {
    background-color: #FFEB3B;
    color: #000;
    font-family: 'Impact', sans-serif;
    padding: 4px 12px;
    transform: skewX(-10deg);
    font-size: 1.1rem;
  }

  /* Details Styles */
  .p4-profile-details {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .p4-meta-box {
    background-color: #1a1a1a;
    padding: 1rem;
    border-left: 4px solid #FFEB3B;
    font-size: 1rem;
  }
  .p4-meta-box strong { color: #FFEB3B; }

  .p4-bio-box h2 {
    font-family: 'Impact', sans-serif;
    text-transform: uppercase;
    color: #FFEB3B;
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    letter-spacing: 0.05em;
  }
  .p4-bio-box p {
    color: #ccc;
    line-height: 1.6;
    font-size: 1.1rem;
  }

</style>